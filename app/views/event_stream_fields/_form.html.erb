<%= form_with(model: event_stream_field) do |form| %>
  <% if event_stream_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_stream_field.errors.count, "error") %> prohibited this event_stream_field from being saved:</h2>

      <ul>
        <% event_stream_field.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :event_stream_id %>
    <%= form.text_field :event_stream_id %>
  </div>

  <div class="field">
    <%= form.label :data_type %>
    <%= form.text_field :data_type %>
  </div>

  <div class="field">
    <%= form.label :stream_type %>
    <%= form.text_field :stream_type %>
  </div>

  <div class="field">
    <%= form.label :required %>
    <%= form.check_box :required %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
