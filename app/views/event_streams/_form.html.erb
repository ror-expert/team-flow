<%= form_with(model: event_stream) do |form| %>
  <% if event_stream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_stream.errors.count, "error") %> prohibited this event_stream from being saved:</h2>

      <ul>
        <% event_stream.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :project %>
    <%= form.text_field :project %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
