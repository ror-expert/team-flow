<%= form_with(model: report_item) do |form| %>
  <% if report_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_item.errors.count, "error") %> prohibited this report_item from being saved:</h2>

      <ul>
        <% report_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :report_id %>
    <%= form.text_field :report_id %>
  </div>

  <div class="field">
    <%= form.label :report_type %>
    <%= form.text_field :report_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
